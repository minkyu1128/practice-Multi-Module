spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: "org.h2.Driver"
    url: "jdbc:h2:mem:testDB;MODE=MySQL"
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
